# simple-banking-app-swing
A simple Java Swing banking application with a modern GUI. Features account creation, deposits, withdrawals, balance checking, and an accounts table using in-memory storage (HashMap).
